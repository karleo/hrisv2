/** Shared login / auth split-panel accent (teal). */
export const LOGIN_BRAND = '#3CA99B';

/**
 * Optional: public URL to a square logo (PNG/SVG). Example: `/images/login-mark.png`.
 * Leave null to use the default app icon in a teal circle.
 */
export const LOGIN_CUSTOM_MARK_PATH: string | null = null;
